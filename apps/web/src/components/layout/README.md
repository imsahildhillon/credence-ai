# layout/

Reserved for future structural primitives (e.g. `Container`, `Stack`, `PageHeader`). None of the components required for this pass (`docs/03-mvp-prd.md` component library task) fit this category — every requested component is a display, form, feedback, or navigation primitive, not a page-layout structural element. Left empty rather than filled with something unrequested.
